[project]
name = "pairnut"
version = "1.1.1"
description = "Local desktop walnut matching tool for merchants."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "PySide6>=6.11.0",
]
